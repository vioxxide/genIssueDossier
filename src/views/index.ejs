<!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Issue Page Generator</title>
      <link href="/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>
      <div class="container mt-5">
        <h1>Generate Issue Page</h1>
        <form action="/generate" method="post">
          <div class="mb-3">
            <label for="topic" class="form-label">Enter an issue:</label>
            <input type="text" class="form-control" id="topic" name="topic" required>
          </div>
          <button type="submit" class="btn btn-primary">Generate</button>
        </form>

        <% if (issues.length > 0) { %>
          <h2>Generated Issues:</h2>
          <ul>
            <% issues.forEach(issue => { %>
              <li><a href="/view/<%= issue._id %>"><%= issue.topic %></a></li>
            <% }) %>
          </ul>
        <% } %>

        <a href="/index" class="btn btn-secondary mt-3">View All Issues</a>
      </div>
    </body>
    </html>
